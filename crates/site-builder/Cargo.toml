[package]
name = "site-builder"
version = "0.1.0"
edition = "2021"
description = "Fast native Rust static site generator for drakestapleton.com"

[dependencies]
crumb-spec = { git = "https://github.com/aien-dev/crumb-spec" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
spark-crumbs = { git = "https://github.com/aien-dev/spark-crumbs" }
